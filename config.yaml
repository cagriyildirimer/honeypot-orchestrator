host: "127.0.0.1"
profile: "empty"

logging:
  path: "logs/events.jsonl"

web:
  enabled: true
  host: "127.0.0.1"
  port: 8000

auth:
  username: "admin"
  password: "admin123"

services:
  http:
    enabled: true
    host: "0.0.0.0"
    port: 8080
  ssh:
    enabled: true
    host: "0.0.0.0"
    port: 2222
  ftp:
    enabled: true
    host: "0.0.0.0"
    port: 2121
  telnet:
    enabled: true
    host: "0.0.0.0"
    port: 2323
